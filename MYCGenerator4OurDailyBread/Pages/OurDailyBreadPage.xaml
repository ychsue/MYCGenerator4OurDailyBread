<Page x:Name="page"
    x:Class="MYCGenerator.Pages.OurDailyBreadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MYCGenerator.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <Border x:Name="bdMain">
                <StackPanel>
                    <TextBlock Margin="4" FontSize="{StaticResource ContentDialogTitleMaxHeight}" HorizontalAlignment="Center" TextWrapping="Wrap"
                               Text="{Binding todayTitle, ElementName=page}"></TextBlock>
                    <Image Margin="4" HorizontalAlignment="Center"
                           Source="{Binding todayImageURL, ElementName=page}"></Image>
                    <Button x:Name="btCreateMYC" Content="Create" Click="btCreateMYC_Click"></Button>
                </StackPanel>
            </Border>

            <Border x:Name="bdSettings">
                <StackPanel>

                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>
